<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialog_box"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:background="@color/colorPurple">

    <ImageView
        android:id="@+id/dialog_box_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:background="@drawable/ic_launcher_background"
        />

    <TextView
        android:id="@+id/dialog_box_title"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dialog_box_image"
        android:text="Lorem Ipsum is simply dummy text of the printing and typesetting industry. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged."
        android:textSize="15dp"
        android:textStyle="bold"
        android:textColor="@color/colorBlack"
        android:background="@color/colorWhite"
        android:paddingBottom="10dp"
        android:paddingTop="10dp"
        android:paddingLeft="7dp"
        />

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="45dp"
        android:layout_below="@+id/dialog_box_title"
        android:gravity="center"
        android:layout_marginTop="2dp"
        >

        <ImageView
            android:id="@+id/dialog_box_twitter"
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:background="@color/colorWhite"
            android:src="@drawable/bluetwitter"
            android:layout_marginRight="1dp"
            />

        <ImageView
            android:id="@+id/dialog_box_bookmark"
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:background="@color/colorWhite"
            android:src="@drawable/ic_bookmark"
            android:layout_marginLeft="1dp"
            />

    </LinearLayout>

</RelativeLayout>